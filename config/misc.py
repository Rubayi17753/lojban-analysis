# Excluding first iteration, which 'loads' the forms from the stack into current_stem
# handle_duplicate_forms_iteration_count = 4

# interactive.new_gismu settings
new_gismu = {
    'path' : 'interactive/new_gismu.csv',
    'sep' : ',',
}